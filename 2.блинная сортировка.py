def bubble_sort(arr):
    n = len(arr)
    # Проходим по всему массиву
    for i in range(n):
        # Внутренний цикл — проходим по оставшейся части массива
        for j in range(0, n - i - 1):
            # Пооперативная проверка соседних элементов
            if arr[j] > arr[j + 1]:
                # Меняем местами, если они в неправильном порядке
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr

# Пример использования:
array = [64, 34, 25, 12, 22, 11, 90]
sorted_array = bubble_sort(array)
print(sorted_array)
#if arr[j] > arr[j + 1]: — сравнивает два соседних элемента
#arr[j], arr[j + 1] = arr[j + 1], arr[j] — при необходимости меняет их местами, «выталкивая» больший элемент в конец массива